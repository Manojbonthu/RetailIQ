services:
  - type: web
    name: retailiq
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --threads 1 --timeout 300 --bind 0.0.0.0:$PORT --max-requests 100
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
